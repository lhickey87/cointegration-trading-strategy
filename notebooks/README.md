# Jupyter Setup

This project uses a local Python virtual environment in `.venv` instead of conda.

Start Jupyter Lab from the project root with:

```bash
source .venv/bin/activate
jupyter lab
```

In Jupyter, choose the kernel named `Python (.venv cointegration)`.
